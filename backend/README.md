# backend

cd backend

# step 1 - creating new virtual environment
python -m venv .venv

# step 2 - activating the environment
.\.venv\Scripts\Activate

# step 3 - installing libs
pip install -r requirements.txt

# step 4 - running the application
python main.py